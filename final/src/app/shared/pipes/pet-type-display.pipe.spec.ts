import { PetTypeDisplayPipe } from './pet-type-display.pipe';

describe('PetTypeDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new PetTypeDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
